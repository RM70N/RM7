{
  "name": "ahsmaha-ai",
  "version": "1.0.0",
  "private": true,
  "description": "احسمها AI — مساعد ذكاء اصطناعي شخصي خاص",
  "type": "module",
  "engines": {
    "node": ">=20.11.0"
  },
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c cyan,magenta \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w server && npm run build -w web",
    "start": "npm run start -w server",
    "db:generate": "npm run db:generate -w server",
    "db:migrate": "npm run db:migrate -w server",
    "db:deploy": "npm run db:deploy -w server",
    "setup": "npm install && npm run db:generate && npm run db:deploy",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w web",
    "lint": "npm run lint -w server && npm run lint -w web",
    "test": "npm run test -w server",
    "test:e2e": "node tests/e2e.mjs && node tests/memory.e2e.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "playwright": "^1.49.1"
  }
}
